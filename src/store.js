import { reactive } from 'vue'

export const store = reactive({
    phoneColor: 'black',
    rotateL: false,
    rotateR: true,
    // rotate: {
    //     rotL: false,
    //     rotR: true
    // }
})